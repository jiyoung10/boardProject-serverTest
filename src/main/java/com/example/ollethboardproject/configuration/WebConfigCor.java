package com.example.ollethboardproject.configuration;

import org.springframework.web.servlet.config.annotation.CorsRegistry;

public interface WebConfigCor {
    void addCorsMapping(CorsRegistry registry);
}
